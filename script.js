let mensagem = "";

//For foi utlizado para o looping de repetição, pois irá fazer um looping onde terá um número exato de repetições
for (let i = 10; i >= 1; i -= 1) {
  mensagem = `${mensagem} ${i}`;

  alert(mensagem);
}
